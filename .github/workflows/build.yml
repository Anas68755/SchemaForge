name: Build SchemaForge Desktop

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch: {}

jobs:
  build:
    strategy:
      matrix:
        os: [windows-latest, macos-latest, ubuntu-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - run: npm install
      - run: npx electron-builder --publish=never
      - uses: actions/upload-artifact@v4
        with:
          name: schemaforge-${{ matrix.os }}
          path: release/*.{exe,dmg,AppImage,zip,blockmap}
          if-no-files-found: ignore
